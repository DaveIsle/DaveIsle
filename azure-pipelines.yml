trigger:
  - Automation  # or your branch

pool:
  vmImage: 'windows-latest'

steps:
- checkout: self

- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '8.x'

- script: dotnet tool restore
  displayName: 'Restore dotnet tools'

- script: dotnet tool run playwright install --with-deps
  displayName: 'Install Playwright browsers'

- script: dotnet restore
  displayName: 'Dotnet restore'

- script: dotnet build --configuration Release --no-restore
  displayName: 'Build'

- script: |
    dotnet test tests/Your.Tests.Project.csproj --configuration Release --no-build \
      --logger "trx;LogFileName=TestResults.trx" --results-directory $(Build.ArtifactStagingDirectory)/test-results
  displayName: 'Run tests'

- task: PublishTestResults@2
  inputs:
    testResultsFormat: 'VSTest'
    testResultsFiles: '$(Build.ArtifactStagingDirectory)/test-results/*.trx'
    failTaskOnFailedTests: true
